"""
=============================================================================
WhalesBot Eagle 함수 테스트 코드
=============================================================================
대회장에서 하나씩 주석 해제하고 테스트하세요.
한 번에 하나의 테스트만 실행! (나머지는 주석 처리)
=============================================================================
"""

from whalesbot import *

# ── 공통 초기화 (항상 필요) ──
fly_unlock()
wait(1)
fly_setspeed(30)
wait(1)
fly_start(80)
wait(3)
fly_hover()
wait(2)

# =============================================================================
# ★ 테스트 1: 전진 50cm
# =============================================================================
# fly_moveto(FRONT, 50)
# wait(3)

# =============================================================================
# ★ 테스트 2: 후진 50cm
# =============================================================================
# fly_moveto(BACK, 50)
# wait(3)

# =============================================================================
# ★ 테스트 3: 좌측 50cm
# =============================================================================
# fly_moveto(LEFT, 50)
# wait(3)

# =============================================================================
# ★ 테스트 4: 우측 50cm
# =============================================================================
# fly_moveto(RIGHT, 50)
# wait(3)

# =============================================================================
# ★ 테스트 5: 상승 30cm
# =============================================================================
# fly_moveto(UP, 30)
# wait(3)

# =============================================================================
# ★ 테스트 6: 하강 30cm
# =============================================================================
# fly_moveto(DOWN, 30)
# wait(3)

# =============================================================================
# ★ 테스트 7: 시계방향 90도 회전
# =============================================================================
# fly_turn(CW, 90)
# wait(3)

# =============================================================================
# ★ 테스트 8: 반시계방향 90도 회전
# =============================================================================
# fly_turn(CCW, 90)
# wait(3)

# =============================================================================
# ★ 테스트 9: AI 태그맵 초기화 + 태그 읽기
# =============================================================================
# AI_TagMapInit(P1, AI_get(P1, AIMODE_APILTAG, 0), 30)
# wait(3)

# =============================================================================
# ★ 테스트 10: AI_GoToTag (태그 번호를 대회장에서 확인 후 입력)
# =============================================================================
# AI_TagMapInit(P1, AI_get(P1, AIMODE_APILTAG, 0), 30)
# wait(3)
# AI_GoToTag(10, 30)
# wait(5)

# ── 공통 착륙 (항상 필요) ──
fly_land()
wait(3)
fly_lock()
