"""
=============================================================================
제3회 국제 창의 AI 드론 경진대회 - 자율비행 미션 코드
=============================================================================
드론: WhalesBot Eagle
환경: from whalesbot import *
=============================================================================
★ 이륙 -> QR태그 인식 -> 태그 기반 장애물 회피
★ fly_moveto(방향, 거리cm) : FRONT, BACK, LEFT, RIGHT, UP, DOWN
★ fly_turn(방향, 각도) : CW, CCW
★ fly_start(고도cm) : 자동이륙
=============================================================================
"""

from whalesbot import *

# ── 초기화 ──
resettime()
fly_unlock()
wait(1)
fly_setspeed(30)
wait(1)

# ── 이륙 ──
fly_start(80)
wait(3)
fly_hover()
wait(2)

# ── 여기서부터 QR태그 인식 후 장애물 회피 코드 추가 ──
# 대회장에서 태그 번호 확인 후 작성


# ── 착륙 ──
fly_land()
wait(3)
fly_lock()
